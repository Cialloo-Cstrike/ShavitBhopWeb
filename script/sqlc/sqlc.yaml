version: "2"
sql:
  - engine: "mysql"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "crud"
        out: "../../backend/crud"
        emit_json_tags: true
        json_tags_case_style: camel